/**
 * Codec utilities — hex dump for debugging binary data.
 *
 * <p>For standard hex encode/decode, prefer the JDK's
 * {@link java.util.HexFormat} (available since Java 17).
 */
package cc.ashclaw.common4j.core.codec;